// qd_Theme.cpp — Q OS desktop color token system for uMenu C++ port (v1.1.12).
// All QdTheme methods are inline static in qd_Theme.hpp; this translation unit
// exists so the Makefile has a .cpp to compile for the qdesktop/ source set
// and so the header is included (and therefore syntax-checked) in the build.
// Ported from tools/mock-nro-desktop-gui/src/theme.rs.

#include <ul/menu/qdesktop/qd_Theme.hpp>
